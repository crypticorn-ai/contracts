# Private key of the wallet that will deploy the contract (without 0x prefix)
PRIVATE_KEY=your_private_key_here

# BSCScan API key for contract verification
BSCSCAN_API_KEY=your_bscscan_api_key_here

# Token address (if deploying staking contract only with existing token)
TOKEN_ADDRESS=your_token_address_here

# Token deployment parameters (for new token deployment)
MARKETING_WALLET=your_marketing_wallet_address
TOKEN_NAME=Crypticorn
TOKEN_SYMBOL=CRYPTO

# Network configuration (uncomment if experiencing ConnectTimeoutError)
# HTTP_PROXY=http://your-proxy-server:port
# HTTPS_PROXY=http://your-proxy-server:port
# NO_PROXY=localhost,127.0.0.1 